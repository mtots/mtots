import gg


final window = gg.Window('geometry', 300, 300)
final rect = window.newPolygon([
  Vector(0,     0),
  Vector(100,   0),
  Vector(100, 100),
  Vector(0,   100),
])
rect.setVertexColor(0, gg.BLUE)
rect.setVertexColor(1, gg.GREEN)
rect.setVertexColor(2, gg.ORANGE)
rect.setVertexColor(3, gg.BROWN)

final pentagon = window.newPolygon([
  Vector(-10, -10),
  Vector( 10, -10),
  Vector( 15,  10),
  Vector(  0,  15),
  Vector(-15,  10),
])
pentagon.setColor(gg.DARK_PEACH)

var angle = 0

@window.onUpdate
def() nil:
  ""
  rect.transform.reset()
  rect.transform.translate(Vector(-50, -50))
  rect.transform.rotateZ(angle)
  rect.transform.translate(Vector(100, 100))
  rect.blit()

  pentagon.transform.reset()
  pentagon.transform.scaleX(2)
  pentagon.transform.scaleY(2)
  pentagon.transform.rotateZ(angle)
  pentagon.transform.translate(Vector(200, 200))
  pentagon.blit()

  angle = angle + 0.1

